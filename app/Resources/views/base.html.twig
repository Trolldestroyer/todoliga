<!DOCTYPE html>
<html lang="es">
<head>

    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.9.1/jquery.min.js"></script>
    <script>window.jQuery || document.write('<script src="{{ asset('js/jquery.min.js') }}"><\/script>')</script>

    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->
    <meta name="description" content="">
    <meta name="author" content="">

    <title>{% block title %}{% endblock %}</title>

    {% block stylesheets %}
        <link rel="stylesheet" href="{{ asset('css/bootstrap.min.css') }}" />
        <link rel="stylesheet" href="{{ asset('css/jumbotron.css') }}" />
        <link rel="stylesheet" href="{{ asset('css/custom.css') }}" />
        <link rel="stylesheet" href="/css/style.css"/>

    {% endblock %}

    <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />

    <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
    <!--[if lt IE 9]>
    <script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
    <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->

</head>

<body class="" style="">


<script>
    var fijar= false;
    $(document).ready(function(){
        $("#menubase").on({
                mouseenter: function(){
                    if(fijar==false) {
                        $(this).css("margin-left", "0px");
                    }},
                mouseleave: function(){
                    if(fijar==false) {
                        $(this).css("margin-left", "-240px");
                    }}, });

            $("#menuinterior").on({
            click: function(){

                if(fijar==false){
                    $(menubase).css("margin-left", "0px");
                    $(menuinterior).css("color", "#88cf58");
                    fijar=true;
                }else{
                    $(menuinterior).css("color", "black");
                    fijar=false;
                }  }
        });
    });

</script>
<div id="menubase" style=" "><div id="menuinterior">📌</div>
            <ul class="col-xs-12 col-sm-12 col-lg-12" style="border: 2px solid rgba(171, 178, 185, 0.1);list-style-type: none; 	background-color: rgba(121, 121, 121,0.3)!important;
-webkit-box-shadow: 5px 8px 10px -2px rgba(0,0,0,1);
	-moz-box-shadow: 5px 8px 10px -2px rgba(0,0,0,1);
	box-shadow: 5px 8px 10px -2px rgba(0,0,0,1);">
                <t class=" col-xs-12 col-sm-12 col-lg-12">
                    <h4 style="text-align:center; color:white; border-bottom: 2px solid rgba(0, 0, 0, 0.3); margin-bottom: 8px!important; padding-bottom: 5px;" class=" col-xs-12 col-sm-12 col-lg-12" >Menu</h4>
                </t>
                <t>
                    <a href="/liga" class=" botonMenuInterior col-xs-12 col-sm-12 col-lg-12" id="milk" >Inicio</a>
                </t>
                {% block sidebar %}{% endblock %}
                {% block menu %}{% endblock %}
                <t>
                    <div style="margin-top: 10px; text-align:center; color:white; border-bottom: 2px solid rgba(0, 0, 0, 0.3); margin-bottom: 8px!important; padding-bottom: 5px;"  class="  col-xs-12 col-sm-12 col-lg-12">Sesión</div>
                </t>
                <t>
                    {{ render_esi(controller('UserBundle:HoritzontalLogin:horitzontalLogin', {'route': app.request.get('_route')})) }}
                </t>

            </ul>
    </div>

<div class="container-fluid" style="z-index: 1;">
    <div class="row">
        <div class="col-md-12">
            {% block body %}{% endblock %}
        </div>
    </div>
    <hr>
    <footer>
        <p style="margin-top: 5px" class="text-right">&copy; 2017 Albert Cerdà</p>
    </footer>
</div> <!-- /container -->

{% block javascripts %}
<script src="{{ asset('js/bootstrap.min.js') }}"></script>
{% endblock %}


</body>
</html>