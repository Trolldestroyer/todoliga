{% extends '::base.html.twig' %}


{% block body %}
    <div class=" fondopartido panel panel-default">
    <h1 class="ligas">Buscador de Ligas</h1>

    {% for Liga in bligas %}
        <div class="cajaliga grisclaro col-lg-3 col-md-3  col-sm-3 col-sm-offset-2 col-xs-10 col-xs-offset-1" style="text-align: center">

            <h3 style=""><a style="text-decoration: none; " class="grisclaro" href="{{ path('app_liga_showLiga', {'slug': Liga.id}) }}">{{ Liga.nombre }}</a></h3>
            <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12" style="margin-top: 10px;">Autor: <a class="verde" href="{{ path('app_usuario_show', {'slug': Liga.creador.id }) }}">{{ Liga.creador }}</a></div>

        </div>
    {% endfor %}
    </div>

{% endblock %}